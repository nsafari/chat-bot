# Chat Bot

Angular chatbot frontend for the RAG Service. See [angular-chatbot/README.md](angular-chatbot/README.md) for setup and usage.

## Quick start

```bash
cd angular-chatbot && npm install && npm start
```
