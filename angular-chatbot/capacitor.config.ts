import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.nsafari.chatbot',
  appName: 'Angular Chatbot',
  webDir: 'dist/angular-chatbot/browser'
};

export default config;
